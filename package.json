{
  "name": "jessee",
  "version": "0.3.5",
  "description": "A native Mac recorder that turns narrated walkthroughs and imported videos into visual, AI-ready documents.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/polyform-ai/jessee.git"
  },
  "bugs": {
    "url": "https://github.com/polyform-ai/jessee/issues"
  },
  "homepage": "https://jessee.ai",
  "type": "module",
  "scripts": {
    "mac:build": "swift build",
    "mac:test": "swift test",
    "mac:typecheck": "tsc --noEmit",
    "mac:editor-test": "node --experimental-strip-types --test tests/macEditorRichText.test.ts",
    "mac:editor": "vite build --config vite.mac-editor.config.ts",
    "mac:app": "npm run mac:editor && zsh mac/Support/build-app.sh",
    "mac:check": "npm run mac:typecheck && npm run mac:editor-test && npm run mac:editor && swift test && zsh mac/Support/build-app.sh",
    "site:build": "node scripts/prepare-website.mjs",
    "site:preview": "npm run site:build && wrangler pages dev ./site-dist",
    "deploy:cloudflare": "npm run site:build && wrangler pages deploy ./site-dist --project-name jessee --branch main",
    "deploy:cloudflare:preview": "npm run site:build && wrangler pages deploy ./site-dist --project-name jessee --branch preview",
    "dev": "vite build --watch --config vite.mac-editor.config.ts",
    "check": "npm run mac:check"
  },
  "dependencies": {
    "@tiptap/core": "^3.31.3",
    "@tiptap/pm": "^3.31.3",
    "@tiptap/starter-kit": "^3.31.3",
    "vite": "^7.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "linkedom": "^0.18.13",
    "typescript": "^5.8.3",
    "wrangler": "^4.133.0"
  }
}
